// Programa de Prueba
    Proc linea1()
        [
            //Define variable local
            Def(varLocal1, 1);
            PosY varLocal1;
        ];
    End;

    Proc posiciona(valorX, valorY)
        [
    For var1(1 to 4) Loop
      [
       Add(valorX, 1);
       PosY valorX; 
      ] End Loop;
        ];
    End;

     //comentario
    Proc main()
        [
            // Define variable global
            Def(varGlobal1, 1);
            //Llama al procedimiento linea1
            linea1();
            //Llama al procedimiento posiciona
            posiciona(1,1);

        ];
    End;